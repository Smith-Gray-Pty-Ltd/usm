---
"@smithgray/usm": patch
---

The MCP tools reference now shows each tool's real name (`usm_read`, `usm_list`, `usm_get_contracts`, …) instead of the internal feature `$id` slug (`mcp-read`, `mcp-list`). The write-tools entry lists all four tools it covers. The shared `command` field's description now covers MCP tool names as well as CLI commands.
